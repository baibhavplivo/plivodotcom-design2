---
interface MetaItem {
  label: string;
  value: string;
}

interface Props {
  companyName: string;
  meta: MetaItem[];
  ctaText?: string;
  ctaHref?: string;
}

const {
  companyName,
  meta,
  ctaText = "Contact Sales",
  ctaHref = "/contact/sales/",
} = Astro.props;
---

<aside class="lg:sticky lg:top-[96px] self-start" aria-label="Company information">
  {/* Company info card */}
  <div class="rounded-lg border border-border p-6">
    {/* Company name as text logo */}
    <div class="font-sora text-xl font-semibold text-foreground mb-6 pb-4 border-b border-border">
      {companyName}
    </div>

    <dl class="space-y-5">
      {meta.map((item) => (
        <div>
          <dt class="text-xs font-medium text-muted-foreground font-mono-ui uppercase tracking-[0.1em] mb-1">
            {item.label}
          </dt>
          <dd class="text-sm text-foreground font-medium">{item.value}</dd>
        </div>
      ))}
    </dl>
  </div>

  {/* CTA card */}
  <div class="rounded-lg bg-surface p-6 mt-4">
    <p class="text-sm text-muted-foreground mb-4">Want similar results for your platform?</p>
    <a
      href={ctaHref}
      class="inline-flex items-center justify-center w-full px-4 py-2.5 text-sm font-medium bg-foreground text-background rounded-md hover:bg-gray-800 transition-colors"
    >
      {ctaText}
    </a>
  </div>
</aside>
